    <header class="w-full h-[8vh] bg-[#023047] text-white flex items-center shadow-md">
        <h2 id="HOME" id="user" class="w-[30%] text-center text-[1.3rem] font-semibold cursor-pointer">
            GILMER
        </h2>
        <h1 id="titulo" class="w-[70%] text-center text-[1.7rem] font-bold">
            Project Manager
        </h1>
     </header>
     <section class="w-full h-[8vh] bg-white gap-4 p-2 flex shadow">
        <input type="text" id="buscarProyecto" placeholder="Proyecto" class="w-[30%] h-full border rounded-2xl px-2"/>
        <input type="text" id="buscarTarea" placeholder="Tarea" class="w-[70%] h-full border rounded-2xl px-2"/>
    </section>

    <script>
    import { home } from "../scripts";

        const HOME = document.getElementById("HOME");
        HOME?.addEventListener('click', ()=> home());
    </script>